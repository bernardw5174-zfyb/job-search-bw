# 00-raw — 原始材料（用户区）

本目录存放**你自己的**原始材料：简历、JD 文本、面试记录、平台截图等。

- ⚠️ **个人数据，`.gitignore` 已排除**——不会被 `git add -A` 跟踪，但请务必**不要 push 到公开 fork**
- 本目录在独立使用场景生效；作为 knowledge-growth 垂直包挂载时，用户数据一律写 `vault/求职/00-raw/`（包内本目录保持空）

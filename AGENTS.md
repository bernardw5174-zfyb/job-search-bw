# AGENTS.md — job-search-bw（独立使用场景）

> **本文件用于独立工作区场景**（本 repo 根目录作为 Agent 工作区打开时生效）。
> 若本目录位于已有根 `AGENTS.md` 的 vault 内（`_packages/` 下），**以根文件为准，本文件不覆盖通用协议**——此时本包为只读参考区，用户数据一律写 vault 工作区（见 `docs/与Core配合使用.md`）。

## 这是什么

国内求职的四环方法包：**诊断 → 关键词 → 改写 → 模拟面试**。7 份框架文档在 `02-框架/`，4 个可执行 skill 在 `skills/`。所有方法的验证状态公开在 `04-复盘/假设注册表-公开版.md`。

**卖的是验证机制，不是结论**：每条方法标注 ✅已验证／⚠️验证中／🔶待验证／❌证伪。引用方法时区分状态，不把"方向性信号"说成"已验证结论"。

## 目录结构

```text
00-raw/            ← 用户材料（简历/JD/面试记录）——个人数据，不入库
01-知识/           ← 用户知识页——个人数据（_drafts/ 不入库）
02-框架/           ← 7 份方法论（只读参考）
03-实战/templates/ ← 模板（只读参考，使用时复制到自己的目录）
03-实战/示例/      ← 合成练习材料（虚构）
03-实战/案例/      ← 案例回流机制说明（首版不收录真实案例）
04-复盘/           ← 假设注册表-公开版 + CHANGELOG（只读参考）
skills/            ← 4 个 skill（任务级加载）
docs/              ← 教程与集成指引
```

## 使用方式

1. 用户提出求职任务（诊断/关键词/改写/模拟面试）→ 加载 `skills/` 对应 skill：
   - `skills/01-resume-diagnosis` — 简历诊断
   - `skills/02-keyword-intelligence` — 关键词对标 / 单 JD 评估
   - `skills/03-resume-rewrite` — 经历改写
   - `skills/04-mock-interview` — 模拟面试
2. skill 内部会指向 `02-框架/` 对应框架文档——**规则以框架文档为准，skill 只管流程**
3. 投递纪律/企业甄别/简历定稿无 skill：作为人工参考框架使用（见 `覆盖度说明.md`）

## 写入纪律

| 内容 | 位置 | 说明 |
|------|------|------|
| 用户材料（简历/JD/投递记录/面试复盘） | `00-raw/` 或用户自己的目录 | **个人数据，`.gitignore` 已排除，绝不 push 到公开仓库** |
| Agent 编译草稿 | `01-知识/_drafts/` | 同上，不入库 |
| 用户确认后的知识页 | `01-知识/` | 同上 |
| 方法/模板引用 | `02-框架/`、`03-实战/templates/` | **只读**：不改、不删、不在包内新建文件；需要修改时复制到自己的目录再改 |

## 隐私纪律

- 本 repo 是公开仓库：**任何写入不得包含真实姓名、公司名、联系方式、薪资、简历原文**
- 用示例材料练习时：`03-实战/示例/` 是合成数据，可放心使用
- 案例回流：只有脱敏、抽象后的共性规律才可提交（走 issue/PR，维护者审核）

## 验证与反馈

- 每条方法的状态看 `04-复盘/假设注册表-公开版.md`（含"验证节奏与停滞标注"规则）
- 发现方法失效/有反例：提 issue 说明场景与证据，维护者核实后更新假设注册表
- 本包单人维护、不定期更新；停更时 README 会标注 archived

# AGENTS.md — job-search-bw（独立使用场景）

> **本文件用于独立工作区场景**（本 repo 根目录作为 Agent 工作区打开时生效）。
> 若本目录位于已有根 `AGENTS.md` 的 vault 内（`_packages/` 下），**以根文件为准，本文件不覆盖通用协议**——此时本包为只读参考区，用户数据一律写 vault 工作区（见 `docs/与Core配合使用.md`）。

## 这是什么

国内求职方法包：**改简历 → 投简历 → 打招呼**。6 份框架文档在 `02-框架/`，4 个可执行 skill 在 `skills/`。所有方法的验证状态公开在 `04-复盘/假设注册表-公开版.md`。模拟面试（面试线）暂未收录（规划中）。

**卖的是验证机制，不是结论**：每条方法标注 ✅已验证／⚠️验证中／🔶待验证／❌证伪。引用方法时区分状态，不把"方向性信号"说成"已验证结论"。

## 目录结构

```text
00-raw/            ← 用户材料（简历/JD/面试记录）——个人数据，不入库
01-知识/           ← 用户知识页——个人数据（_drafts/ 不入库）
02-框架/           ← 6 份方法论（只读参考）
03-实战/templates/ ← 模板（只读参考，使用时复制到自己的目录）
03-实战/示例/      ← 合成练习材料（虚构）
03-实战/案例/      ← 案例回流机制说明（首版不收录真实案例）
04-复盘/           ← 假设注册表-公开版 + CHANGELOG（只读参考）
skills/            ← 4 个 skill（任务级加载）
docs/              ← 教程与集成指引
```

## 使用方式

0. **首次使用判断**：用户开始求职任务（诊断/对标/改写）时先检查工作区——`00-raw/` 无 JD 语料且 `01-知识/` 无词表页 = 首次使用 → **起始环节选项必须包含「跑词频」**（收集 10-15 份目标 JD → `skills/02-keyword-intelligence` 频率分析建词表），且**列在第一位**，其余选项（简历诊断 / JD 匹配 / 经历改写）排后；得到词表后再进入诊断/对标；用户只有 1 份 JD 或想快速试用 → 允许降级（单 JD 对标/示例材料练习），并提示"词表未校准，对标是近似的"
1. 用户提出求职任务（诊断/关键词/改写）或丢来材料（微信文章/URL/JD）→ 加载 `skills/` 对应 skill：
   - `skills/05-material-ingest` — 材料接收（微信文章/URL/附件 → 处理 → 引导去向）
   - `skills/01-resume-diagnosis` — 简历诊断
   - `skills/02-keyword-intelligence` — 关键词对标 / 单 JD 评估
   - `skills/03-resume-rewrite` — 经历改写
2. skill 内部会指向 `02-框架/` 对应框架文档——**规则以框架文档为准，skill 只管流程**
3. 投递纪律/企业甄别/简历定稿/打招呼话术无 skill：作为人工参考框架/模板使用（见 `覆盖度说明.md`）
4. 用户说"帮我登记投递"→ 读 `02-框架/投递流程框架-国内版.md` → **查重后**追加一行到 `00-raw/投递记录/投递记录表.csv`（无此文件则先复制模板）

## 材料与链接处理（微信文章 / URL / 附件）

用户丢来微信文章、URL 或附件时（求职语境最常见输入），按以下流程处理：

1. **先处理，不追问**：读取 / 抓取正文，消化后给出——这篇讲了什么、与求职的关系、AI 的判断与不确定项；
2. **后用选项问去向**：存进知识库 / 不用存 / 提炼框架候选 / 其他（人工输入）；
3. **若存**：
   - 确认领域（默认求职）→ 落 `00-raw/`（微信/网页抓取一律标「**非字节级** + 抓取时间戳 + 原文 URL」，不得冒充原始文件）；
   - 在 `01-知识/_drafts/` 生成草稿，**停在草稿等你确认**，任何档位不自动晋升；
4. **识别为招聘信息（JD）**：提示可走 `skills/02-keyword-intelligence` 做关键词对标 / 单 JD 评估；**每累积约 10 份 JD 提示一次重算词表**（语料驱动，见 `skills/05-material-ingest`）；
5. **版权边界**：外部文章（公众号/网页）仅存私有工作区（`00-raw/` 已被 `.gitignore` 排除），**不 push 公开仓库**；不把文章内容复制进 `02-框架/` 或 skills；
6. **框架候选门**：文章不能直接成为框架依据——先入库、你确认后，再走框架提案流程。

## 产出物交付（默认对话交付）

- **默认对话交付**：分析/评估/话术/建议等产出，默认在对话中交付（用户复制即用），**不落盘**——用户没要求落盘就不写文件
- **落盘仅限框架明文指定**：简历档案（`00-raw/简历/`）、投递记录（`00-raw/投递记录/投递记录表.csv`）、岗位词表（`01-知识/`）、外部材料（`00-raw/`）——这些是框架明文指定位置的产物，照常落盘
- **用户明确要求落盘**才落盘（如"存下来/落盘/写文件"）
- **判断顺序**：先问"这件事需要文件吗"，再问"放哪"——不要默认"交付＝产出文件"

## 写入纪律

| 内容 | 位置 | 说明 |
|------|------|------|
| 用户材料（简历/JD/投递记录等） | `00-raw/` 或用户自己的目录 | **个人数据，`.gitignore` 已排除，绝不 push 到公开仓库** |
| 投递记录 | `00-raw/投递记录/投递记录表.csv`（复制 `03-实战/templates/投递记录表.csv`） | 同上，不入库；投完顺手填一行 |
| Agent 编译草稿 | `01-知识/_drafts/` | 同上，不入库 |
| 用户确认后的知识页 | `01-知识/` | 同上 |
| 岗位词表（对标基准） | `01-知识/`（如 `AI应用方向-词表.md`） | 同上；重算时旧词表标 `superseded_by`，不删历史 |
| 方法/模板引用 | `02-框架/`、`03-实战/templates/` | **只读**：不改、不删、不在包内新建文件；需要修改时复制到自己的目录再改 |

## 备份纪律（覆盖前先备份的落点规则）

需要覆盖或修改用户内容前、或为跨库操作留底时，Agent 可能执行「先备份」——**备份落点必须满足以下规则，否则不执行**：

1. **只允许落在忽略区内**：备份落点必须在 `.gitignore` 覆盖范围内（如 `00-raw/` 下新增子目录）。禁止落在工作区根目录、`docs/`、`02-框架/`、`03-实战/templates/`、`skills/` 等**包受控目录**——这些目录会被提交。
2. **根目录不放置任何个人内容**：`00-raw/` 之外（尤其仓库根目录）不得放置任何含个人信息的备份（简历、JD、聊天记录、导出文件）。本包是**公开仓库**：根目录个人文件一旦提交 = 对外泄露。
3. **字节级备份进 Raw**：备份应为字节级副本，放 `00-raw/`（同「材料与链接处理」的字节级口径），文件名保留来源标识与日期。
4. **不确定落点就问**：找不到合规落点时，用交互式选项问用户指定，不得自行选一个包受控目录存放。

## 隐私纪律

- 本 repo 是公开仓库：**任何写入不得包含真实姓名、公司名、联系方式、薪资、简历原文**
- 用示例材料练习时：`03-实战/示例/` 是合成数据，可放心使用
- 案例回流：只有脱敏、抽象后的共性规律才可提交（走 issue/PR，维护者审核）

## 验证与反馈

- 每条方法的状态看 `04-复盘/假设注册表-公开版.md`（含"验证节奏与停滞标注"规则）
- 发现方法失效/有反例：提 issue 说明场景与证据，维护者核实后更新假设注册表
- 本包单人维护、不定期更新；停更时 README 会标注 archived
